# SkyBoot Core API - Development Dockerfile
# Python 3.12 기반 개발 환경 컨테이너

# 베이스 이미지: Python 3.12 slim 버전 사용
FROM python:3.12-slim

# 메타데이터 설정
LABEL maintainer="SkyBoot Core Team"
LABEL version="1.0.0-dev"
LABEL description="SkyBoot Core API Development Container"

# 환경 변수 설정 (개발 환경용)
ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PYTHONPATH=/app \
    PIP_NO_CACHE_DIR=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1 \
    ENVIRONMENT=development

# 시스템 패키지 업데이트 및 개발 도구 설치
RUN apt-get update && apt-get install -y \
    gcc \
    g++ \
    libpq-dev \
    curl \
    vim \
    git \
    && rm -rf /var/lib/apt/lists/*

# 작업 디렉토리 설정
WORKDIR /app

# 필요한 디렉토리 생성
RUN mkdir -p /app/logs /app/uploads

# Python 의존성 파일 복사 및 설치
COPY requirements.txt .
RUN pip install --upgrade pip && \
    pip install -r requirements.txt && \
    pip install pytest-cov black flake8 mypy

# 애플리케이션 코드 복사
COPY . .

# 포트 노출
EXPOSE 8000

# 개발 서버 실행 (리로드 활성화)
CMD ["python", "-m", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]